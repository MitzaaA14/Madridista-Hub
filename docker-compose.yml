version: '3.8'

services:
  db:
    image: postgres:16
    container_name: realmadrid_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: RealMadridPassword2026
      POSTGRES_DB: realmadrid_cms
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:



